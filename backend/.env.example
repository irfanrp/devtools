# Backend environment variables for local development (example)
# Copy this to backend/.env and adjust values as needed.

# Port the backend listens on
PORT=8080

# Environment (development|production)
ENV=development

# Logging level (debug|info|warn|error)
LOG_LEVEL=debug

# Enable server-side auto-fix behavior (true/false)
AUTO_FIX_ENABLED=true

# Note: do not commit secrets to this file in real projects. Use a secrets manager for
# production credentials.
