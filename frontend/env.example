# Frontend environment variables (build-time example)
# Copy to frontend/.env and set NEXT_PUBLIC_API_URL as needed for local dev.

# If set, the frontend will use this API base URL instead of the default fallbacks.
# Example: NEXT_PUBLIC_API_URL=http://localhost:8080
NEXT_PUBLIC_API_URL=

# Build environment: development or production
NODE_ENV=development

# Note: this file is safe to commit; do not store secrets here.
